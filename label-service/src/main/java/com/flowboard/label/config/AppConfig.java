package com.flowboard.label.config;

import org.springframework.context.annotation.Configuration;

// RestTemplate removed — inter-service calls use Feign Clients with Eureka lb:// routing.
@Configuration
public class AppConfig {
}
